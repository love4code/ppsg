<div class="container py-5">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-3"><%= project.title %></h1>
            
            <% if (project.mainImage) { %>
                <img src="/admin/media/image/<%= project.mainImage._id %>/thumbnail" class="img-fluid rounded mb-4" alt="<%= project.title %>" loading="lazy" decoding="async">
            <% } %>

            <div class="mb-4">
                <p class="lead"><%= project.description %></p>
            </div>

            <% if (project.location) { %>
                <p><strong>Location:</strong> <%= project.location %></p>
            <% } %>

            <% if (project.date) { %>
                <p><strong>Date:</strong> <%= new Date(project.date).toLocaleDateString() %></p>
            <% } %>

            <% if (project.tags && project.tags.length > 0) { %>
                <div class="mb-4">
                    <strong>Tags:</strong>
                    <% project.tags.forEach(tag => { %>
                        <span class="badge bg-secondary me-1"><%= tag %></span>
                    <% }); %>
                </div>
            <% } %>

            <% if (project.gallery && project.gallery.length > 0) { %>
                <h3 class="mt-5 mb-3">Gallery</h3>
                <div class="row g-3">
                    <% project.gallery.forEach(image => { %>
                        <div class="col-md-6">
                            <img src="/admin/media/image/<%= image._id %>/thumbnail" class="img-fluid rounded" alt="Gallery image" loading="lazy" decoding="async">
                        </div>
                    <% }); %>
                </div>
            <% } %>
            
            <div class="mt-4">
                <a href="/projects" class="btn btn-primary">Back to Projects</a>
            </div>
        </div>
    </div>
</div>

